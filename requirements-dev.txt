# Development dependencies for AutomatedFanfic
# These dependencies are needed for testing, development, and CI

# Include all runtime dependencies
-r requirements.txt

# Testing framework and utilities
freezegun==1.5.5
httpx>=0.28.1
parameterized==0.9.0

# Code quality and pre-commit hooks
pre-commit==4.6.0
black>=26.5.1
ruff>=0.15.16

# Build tools (may be needed for some development scenarios)
setuptools==83.0.0
